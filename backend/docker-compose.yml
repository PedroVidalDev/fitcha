services:
  api:
    build:
      context: .
    container_name: fitcha_api
    restart: always
    env_file:
      - .env
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    networks:
      - fitcha-network

networks:
  fitcha-network:
